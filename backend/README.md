# Reference Authenticator Backend

`server.py` serves the frontend and API from the same local application.

Run locally:

```bash
cd backend
pip install -r requirements.txt
python server.py
```

Copy `env.example` to `.env` and add API keys before running if AI fallback is required.
